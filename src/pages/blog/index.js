import React from 'react';
import Wrapper from "../../hoc/Wrapper";

const Blog = () => {
    return (
        <Wrapper title='Blog'>
            <h1>Blog</h1>
        </Wrapper>
    );
};

export default Blog;